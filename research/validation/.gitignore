# Validation reports generated from the production database are operational
# data and stay out of git (same policy as reports/). Only the synthetic
# SAMPLE-* report, generated from fabricated fixture data, is committed.
*
!.gitignore
!SAMPLE-*
